{
  "name": "blockiverse-vr-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "test:links": "node scripts/check-external-link-targets.js",
    "start": "eleventy --serve --port 8765"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
